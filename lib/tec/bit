void makes(int a, int b, int v) {
    if(a == b) {
        cout << a << " ";
        return;
    }
    int x = (a ^ b) & (-(a ^ b));
    v ^= x;
    int y = v & (-v);
    makes(a, a ^ y, v);
    makes(a ^ y ^ x, b, v);
}